<section class="blogs">
    {{#each blogs as |blog|}}
    <section class="card">
        <header>{{blog_title}}</header>
        <article>{{blog_body}}</article>
    </section>
    {{/each}}
</section>